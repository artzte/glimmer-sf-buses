'use strict';

module.exports = function(environment) {
  let ENV = {
    modulePrefix: 'sf-buses',
    environment: environment,
    rootURL: '/',
    locationType: 'history',
  };

  return ENV;
};
